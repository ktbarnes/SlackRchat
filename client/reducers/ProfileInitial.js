export default ProfileInitial = (state = [], action) => {
  switch(action.type) {
    case 'ADD_USER_PROFILE':
      return [state, 
        { id:
          

        }

      ]

      


      
      default: 
        return state;
    }
}